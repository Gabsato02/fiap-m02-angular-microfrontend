# FiapM02AngularMicrofrontend

Projeto feito com o framework [Angular CLI](https://github.com/angular/angular-cli) version 17.3.11.

## Development server

Executar `ng serve dashboard`, `ng serve auth` e por fim `ng serve shell -o`. 
